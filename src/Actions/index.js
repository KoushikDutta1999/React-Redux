const CounterTypes = {
  INCREASE_COUNT: "INCREASE_COUNT",
  DECREASE_COUNT: "DECREASE_COUNT",
};

export default {
  ...CounterTypes,
};

// {ActionType:value}

// ... -> spread operator
// we copy object with ...
// object cloning
